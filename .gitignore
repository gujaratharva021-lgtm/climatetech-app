# Secrets — never commit. Matches at any depth (backend/.env,
# backend/firebase-service-account.json, and this directory's own .env).
.env
firebase-service-account.json

*.bak
*.bak2
backend/climatetech_backend_linux
backend/climatetech_migrate_linux
